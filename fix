no error when wrong pass and username

add notification bell every page in admin

i have changes in the students accounts when student is added in the system instead of MCC@firstname make the username as there student id then the password is still the last 5 digit

make the remember me working 

if logged out then browser button is clicked dont allow it for the safety

the change password in the student jassu csu teacher is still not working right now

inside the user-management i want you to separate the student accounts and remove the delete in the students so the account cant be deleted

then in the student records if the id is input again even if it exist say a error message that this id is already taken

then in the install instead of poping up as a button below the log in make it an al


